#Disable batch job's auto start 
# spring.batch.job.enabled=false
spring.main.banner-mode=off
 
#batch input files location
# input.dir=c:/temp/input

# Run H2DB on port 8090
server.port = 8090